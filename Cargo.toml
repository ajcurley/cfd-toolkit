[package]
name = "toussaint"
version = "0.1.0"
edition = "2021"

[lib]
name = "toussaint"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.21.1", features = ["extension-module"] }

[profile.dev]
opt-level = 3
